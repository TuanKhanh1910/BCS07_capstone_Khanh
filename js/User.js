// tạo 1 lớp đối tượng
function User(_email, _pass, _name, _gender, _phone) {
  (this.email = _email),
    (this.password = _pass),
    (this.name = _name),
    (this.gender = _gender),
    (this.phone = _phone);
}
